{
  "private": true,
  "name": "chumbucket-hackathon26",
  "description": "Root scripts delegate to frontend/ — run npm install in frontend/ first, or use npm run dev from here.",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
